#import RPi.GPIO as GPIO
#import array
#import bitstring
#import serial
import sys
#import time

#lsusb
#find usb devices with ls /sys/bus/usb-serial/devices/ -ltrah
ser = serial.Serial ("/dev/ttyAMA0")    #Open named port 
ser.baudrate = 9600                     #Set baud rate
#data = ser.read(10)                     #Read ten characters from serial port to data
#ser.write(data)                         #Send back the received data
ser.close()   

print(sys.version_info)

#ser = serial.Serial("/dev/ttyUSB0") #open port
#ser.baudrate = 38400
#hex_string = "56002600"
#hex_data = hex_string.decode("hex")
#print hex_data
##array.array('B', hex_data)
#command = bytearray(hex_data)
#num_bytes_written = ser.write(command)
#print num_bytes_written
#data = ser.read(4)
#print data
#ser.close()
#
#
#decode_data = hex_data.decode()
#print hex_data
#
command = bytearray.fromhex('56002600')
print('The byte array version is = ')
print(command)
#print len(command)
for d in command:
    print(hex(d))

